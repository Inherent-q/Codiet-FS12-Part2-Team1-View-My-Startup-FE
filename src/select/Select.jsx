// src/compare/Compare.jsx

import { useState, useEffect } from "react";
import { getSessionId, getOrCreateSessionId } from "../utils/sessionManager";

const API_BASE_URL =
  import.meta.env.VITE_API_URL || "http://localhost:3000/api";

export const Select = () => {};

export default Select;
